import 'package:flutter/material.dart';

class Matrimonoy_1 extends StatelessWidget {
  const Matrimonoy_1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Column(
          children: [
            Stack(children: [],),

          ],
        ),
      ),
    );
  }
}