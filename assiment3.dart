import 'package:flutter/material.dart';

class Assiment3 extends StatelessWidget {
  const Assiment3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
